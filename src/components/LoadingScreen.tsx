import React from 'react';
import { motion, AnimatePresence } from 'motion/react';
import { useConfig } from '@/contexts/GlobalConfigContext';

export const LoadingScreen: React.FC<{ isReady: boolean }> = ({ isReady }) => {
  return null;
};
